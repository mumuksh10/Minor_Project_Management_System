export const loginTeacher = (
  credentials,
  history,
  setFieldError,
  setSubmitting
) => {
  const user = {
    name: "Rahul Saxena",
    email: "rahul.saxena@jaipur.manipal.edu",
  };
  const status = true;

  if (status === true) {
    // Allow access and redirect
  } else {
    //Return error to the user
  }
};
export const loginStudent = (
  credentials,
  history,
  setFieldError,
  setSubmitting
) => {};
